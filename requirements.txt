# 运行本 Agent 所需的全部第三方依赖（版本与开发环境一致，确保可复现）
# 安装：pip install -r requirements.txt
openai==3.3.1
python-dotenv==1.2.3
PyYAML==6.0.3
mcp==2.0.0
# Web UI（Emperor Agent，任务 A1 起）
fastapi==0.141.1
uvicorn[standard]==0.52.4
