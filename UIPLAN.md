# Emperor Agent · 前端 UI 计划书（UIPLAN.md）

> **软件名**：Emperor Agent（皇帝的 Agent）
> **定位**：把现有终端 Agent 包装成一个有古代宫廷视觉的桌面软件——
> 用户是皇上，Agent 是太监总管，对话即朱批与奏折的往来。
> 创建：2026-08-24 ｜ 状态：计划阶段，未动工
> 纪律沿用 PLAN.md：只做下一个未勾选项；勾选以【完成标志】实测为据；改动留痕；新想法进 Backlog。

---

## 一、总体架构

```
┌──────────────────────────────────────────────┐
│ Emperor Agent 桌面窗口（pywebview 壳，阶段 D） │
│  ┌────────────────────────────────────────┐  │
│  │ 前端页面：纯 HTML/CSS/JS（零构建）        │  │
│  │  WebSocket 收事件流 ←→ 发消息/操作       │  │
│  └───────────────┬────────────────────────┘  │
└──────────────────┼───────────────────────────┘
                   │ ws://127.0.0.1:8300/ws
┌──────────────────┴───────────────────────────┐
│ FastAPI 服务（阶段 A，复用 agent_core 全部能力）│
│  SessionRunner：把 main.py 的 REPL 主循环抽成  │
│  可编程驱动的事件流（token/工具/派遣/门禁…）    │
│  终端版 main.py 保留，双入口共存               │
└──────────────────────────────────────────────┘
```

**选型理由**：
- **FastAPI + uvicorn**：async 天然适配流式事件；Python 生态标准，与现有代码同语言
- **纯 HTML/CSS/JS，不用任何前端框架/构建链**：不引入 Node/npm，每个文件初学者都能读懂；
  单页面 + CSS 变量足够支撑宫廷主题（复杂度到顶再升级，升级进 Backlog）
- **pywebview 桌面壳**（阶段 D）：纯 Python 打包成窗口软件；Tauri 要 Rust、Electron 要 Node，均不取
- **只绑 127.0.0.1**：这个 Agent 能执行命令、读写文件，**绝不监听局域网**——这是红线，写进验收

**新增依赖**（届时锁版本入 requirements.txt）：`fastapi`、`uvicorn[standard]`、`pywebview`（D 阶段）。

---

## 二、视觉设计规范

### 命名与隐喻（UI 词汇 ↔ 功能）

| UI 元素 | 隐喻 | 对应功能 |
|---|---|---|
| 顶栏 | 宫檐 | 标题 Emperor Agent、状态灯、退朝（退出） |
| 主聊天区 | 金銮殿 | 对话流 |
| 用户消息 | 朱批（右对齐、朱砂色短卡） | 用户输入 |
| Agent 回复 | 奏折卷轴（左对齐、展开式长卡） | 流式回复 |
| 工具执行 | 内务府条目（可折叠小卡） | 工具调用过程与结果 |
| 高危确认 | 圣旨与玉玺（模态弹窗） | HookDecision ask → 准奏/驳回 |
| 加载状态 | 宫灯（摇曳动画） | 生成中 |
| 会话列表 | 偏殿名册 | /new、/resume |
| 计划清单 | 差事灯笼（[ ]挂起 [~]点亮 [x]落款） | todos 实时状态 |
| 队友 | 班底名册 | /team 面板 |
| 人格 | 换装间 | /persona 切换 |
| 子代理派遣 | 小太监出巡（通知卡，含熔断回禀） | dispatch 事件 |

### 色板与字体（CSS 变量，先行定义于 B1）

| 变量 | 色值 | 用途 |
|---|---|---|
| `--xuanmo` | `#171210` | 玄墨底色（页面背景） |
| `--gongqiang` | `#5e1f18 → #2a0f0b` 渐变 | 宫墙红（侧栏/卡片底） |
| `--liujin` | `#d4af37` | 鎏金（标题、边框、强调） |
| `--minghuang` | `#e8c26a` | 明黄（正文亮字） |
| `--zhusha` | `#b93a32` | 朱砂（朱批、危险操作） |
| `--qingyu` | `#7ba697` | 青玉（成功、正常状态） |

字体：标题用 `KaiTi, STKaiti, serif`（楷体）；正文 `宋体/系统 serif`。
装饰：回纹边框、卷轴头尾、玉玺印（全部 CSS/SVG 实现，不引入图片资产）。

### 布局草图

```
┌──────────────────────────────────────────────────────┐
│ ⛩ Emperor Agent                        ● 宫灯·当值   退朝│
├───────────┬──────────────────────────────────────────┤
│ 偏殿名册   │      ┌─ 奏折 ────────────────┐            │
│  ├ 乾清宫  │      │ 奉天承运皇帝诏曰……     │            │
│  │ (当前)  │      └────────────────────────┘            │
│  ├ 0823夜 │  ┌─ 朱批 ─────┐                          │
│  └ 0824晨 │  │ 查查跑分榜单 │  （右对齐）                │
│ ─────────  │  └────────────┘                          │
│ 差事灯笼   │  ▸ 内务府·read_file main.py ✓（可折叠）   │
│  ○ 查榜单  │  ▸ 小太监#3 出巡查访…（宫灯动画）          │
│  ◐ 汇总   │      ┌─ 圣旨待批 ─────────────┐            │
│ ─────────  │      │ 皇上，此令需朱批：       │            │
│ 班底名册   │      │ [准奏]      [驳回]      │            │
│ 换装间     │      └─────────────────────────┘            │
├───────────┴──────────────────────────────────────────┤
│ 【传旨……                                        】 传旨 │
└──────────────────────────────────────────────────────┘
```

---

## 三、任务清单（阶段 A→D，共 12 项）

### 阶段 A：地基——后端服务化（动 UI 之前先把骨架立起来）

- [x] **A1 FastAPI 服务骨架**（✅ 2026-08-24，实测：/api/health 返回 ok、占位页伺服正常、netstat 确认仅监听 127.0.0.1:8300 无 0.0.0.0 绑定、关停后端口释放；fastapi==0.141.1 / uvicorn[standard]==0.52.4 已锁版本）
  - 要求：`web/server.py` 提供 `/`（静态页占位）与 `/api/health`；只绑 127.0.0.1；
    uvicorn 启动打印地址；requirements 锁版本
  - 【完成标志】浏览器打开占位页；curl /api/health 返回 ok；`netstat` 确认只监听本机回环
- [x] **A2 SessionRunner：主循环与终端解耦**（✅ 2026-08-24，终端回归：多步工具任务输出格式与旧版一致、全部斜杠命令正常；脚本驱动：7 类事件齐全、历史连续、会话文件全保真。顺手修正：拦截回复的硬编码太监前缀改为人格中性文案，与 4.6 对齐）
  - 要求：把 main.py 的对话驱动抽为可编程调用的 runner（复用 call_llm/registry/记忆/压缩全套），
    终端版 main.py 改为调用同一实现，双入口共存；不改变任何工具行为
  - 【完成标志】终端版回归实测（一轮含工具调用的对话）；脚本直接驱动 runner 跑通一轮对话
- [x] **A3 WebSocket 事件流协议**（✅ 2026-08-24，实测：token 10 段增量拼接与 done.reply 完全一致；工具事件链完整；ask 准奏/驳回/超时三分支全对（超时 3s 自动驳回 fail-closed）；终端非交互默认拒绝零回归。附带修复：server.py 自举 sys.path，任意目录可启动。**阶段 A 完成**）
  - 要求：定义事件类型 `token / tool_start / tool_end / subagent / todos / hook_ask /
    hook_decision / done / error / session`；runner 产出事件，ws 推送；hook ask 从阻塞 input()
    改为事件+等待确认（超时默认驳回，fail-closed 不变）
  - 【完成标志】ws 测试脚本收到 token 增量序列且能完整拼回回复；模拟 ask 事件回"准奏/驳回"
    两分支行为正确；终端版 ask 行为不回归

### 阶段 B：门面——宫廷风前端

- [x] **B1 视觉基调与整体框架**（✅ 2026-08-24，浏览器实测：宽屏六项视觉检查全过（玉玺/宫檐/四面板/朱批/卷轴/传旨栏）；窄视口结构不塌（600×700 无重叠溢出），顶栏换行修复；零图片资产确认。**验收抓到并修复真 bug：静态目录未挂载致 style.css 404 页面裸奔**——改用 StaticFiles 挂根（挂在全部路由之后）；遗留小项：600px 下"当值"灯不显眼，B3 打磨）
  - 要求：按第二节规范落地 CSS 变量、宫檐顶栏、偏殿侧栏、金銮殿聊天区、传旨输入框；
    回纹边框与卷轴卡片（纯 CSS/SVG）
  - 【完成标志】静态页即有宫廷气质（无功能也成立）；无图片资产；窗口缩放布局不塌
- [x] **B2 对话核心流**（✅ 2026-08-24，实测：流式逐字上屏 19 个增长台阶（9→288字）；工具任务全流程可见（内务府卡含输入与输出）；断线提示+3秒自动重连+重连后恢复应答；截图六项视觉核对全过。**验收修复两个真 bug**：①.veil 的 display:flex 覆盖 hidden 属性致遮罩拦截全部点击（视觉验收看不出、交互验收即现形）；②测试期间旧标签页/浏览器缓存致新旧页面混淆——教训：改前端后要穿透缓存重开页）
  - 要求：连 ws 发消息；token 流式渲染成奏折；朱批右对齐；工具执行折叠卡（可展开看结果）；
    宫灯"当值/行走中"状态
  - 【完成标志】真实对话逐字上屏；一轮含工具调用的任务全流程可见；断线有提示
- [x] **B3 交互完备**（✅ 2026-08-24，实测：多步任务灯笼四态联动（计划○→执行◐→全勾◉）；上翻即停跟随+回底钮点击恢复；600 字长文叫停后仅收 13 字部分回复快速收束；誊抄"已誊抄 ✓"。注：原行漏写复选框，本次一并补正。**阶段 B 完成**。踩坑两记：浏览器咬旧 app.js——资源 URL 版本号根治；Playwright 合成 press 时灵时不灵——自动化输入优先 CUA 物理路径）
  - 要求：自动滚动与"回看暂停"；停止生成；复制奏折内容；todos 灯笼实时更新（吃 todos 事件）
  - 【完成标志】一次多步骤任务（拆计划→逐步执行→全勾）在 UI 中完整走完且灯笼联动

### 阶段 C：朝政设施——面板与管控

- [x] **C1 圣旨确认弹窗（ask 的 UI 化）**（✅ 2026-08-24，四场景全过：浏览器实测弹窗弹出（原因+命令+120秒倒计时）、点驳回→拦截；服务端注入法验证准奏→命令真实执行（blocked=False）、超时→自动驳回；终端非交互默认拒绝零回归。验证备注：浏览器自动化输入间歇失灵，回执链路改用 Python ws 客户端注入验证——UI 弹窗与回执协议各自独立取证）
  - 要求：hook_ask 事件 → 模态"圣旨待批"（准奏/驳回/超时倒计时默认驳回）；结果回传后端
  - 【完成标志】让 Agent 执行高危命令（如 git push）弹窗；准奏则执行、驳回则 Agent 收到
    拒绝原因并如实回禀
- [x] **C2 侧殿面板群**（✅ 2026-08-24，注入法验证：跨连接会话闭环（建殿→开新殿→恢复旧殿→续接上下文）、人格切换生效（管家用语）、未知人格报错；浏览器验证：偏殿名册/换装间/班底名册/记忆卷宗四面板真数据装载。踩坑：ws 事件流里 todos/idle 等伴随事件会插队，测试脚本必须过滤等待而非按序断言）
  - 要求：偏殿名册（会话列表 + 开新殿/入旧殿，对应 /new /resume）；差事灯笼；记忆卷宗
    （MEMORY/USER 只读查看）；换装间（人格切换）；班底名册（队友状态）
  - 【完成标志】各面板数据与终端版一致；新建/恢复会话、切人格在 UI 内闭环生效
- [x] **C3 子代理与 MCP 可视化**（✅ 2026-08-24，实测：正常派遣回禀卡含内容、熔断派遣显示"已熔断收兵"⚡卡与原因、出巡簿两条记录（熔断 0✓/3✗ 高亮 / 办妥 1✓/0✗）、外务府面板列出 demo server 与工具。**阶段 C 完成**。备注：subagent_summary 事件补 summary 字段；注入测试等 done 需放宽超时——子代理熔断后主 agent 还要多轮汇报）

### 阶段 D：成软件——打包与分发

- [x] **D1 pywebview 桌面壳**（✅ 2026-08-24，实测：一条命令弹出"Emperor Agent · 金銮殿"窗口（1280×860）、服务健康、ws 对话可用（11 段 token 流式）；关窗后端口释放、python 进程 0、MCP 子进程 0。开发中修复：猴子补丁 serve 签名不兼容 → 改用端口轮询就绪。注：图标留待 D2 打包时随 exe 资源一起做）
- [x] **D2 打包 exe**（✅ 2026-08-24，实测：38.2 MB 单文件，配 .env 后双击 **4 秒**起窗进对话（ws 流式验证）；无 .env 首启弹引导不崩溃；关窗端口/进程/MCP 子进程零残留。产物：`pyinstaller emperor.spec` → dist/EmperorAgent.exe；图标 assets/emperor.ico（PIL 绘制多尺寸玉玺方印）；build/dist 已入 .gitignore）
- [x] **D3 文档与门面收尾**（✅ 2026-08-24，截图入库 docs/images/hall.png 与 decree.png；README 增补软件形态（双入口/打包/安全红线）；SUMMARY 收录 UI 阶段任务表、知识点第 7 节、自测题 34-38、踩坑 #10-14、数字更新。**至此 UIPLAN 12 项全部完成，Emperor Agent 全旅程收官**）

---

## 四、学习路线（与任务绑定）

| 任务 | 知识点 | 学会的检验 |
|---|---|---|
| A1 | HTTP 服务、REST、本机回环与安全边界 | 能说清为什么只绑 127.0.0.1 |
| A2 | 关注点分离：IO 外壳与内核解耦 | 能画出双入口共用内核的结构图 |
| A3 | WebSocket、事件驱动架构、异步确认的桥接 | 能枚举 10 类事件并说明谁发谁收 |
| B1 | CSS 变量主题系统、布局（flex/grid）、SVG 纹样 | 能只改变量换一套配色 |
| B2 | DOM 流式渲染、事件消费、断线重连 | 能解释前端如何拼 token 流 |
| B3 | 滚动锚定、状态管理、中断请求 | 能处理"用户上翻时暂停自动滚动" |
| C1 | 异步确认闭环（事件→弹窗→回执） | 能说清 ask 从阻塞到异步改造的风险点 |
| C2 | 面板数据同步、REST+WS 混合 | 能决定某数据该拉取还是推送 |
| C3 | 可视化可观测性（把日志变成界面） | 能为一类新日志设计展示组件 |
| D1 | 桌面壳、进程生命周期管理 | 能列出关窗时必须回收的资源 |
| D2 | 打包、依赖裁剪、首启配置引导 | 能解释 .env 为什么绝不打进 exe |
| D3 | 文档与作品意识 | 截图能一眼讲清软件是什么 |

---

## 五、风险与红线（先立规矩）

1. **只监听 127.0.0.1**（红线）：Agent 有命令执行能力，任何阶段都不暴露局域网；每阶段验收含此项
2. **终端版共存**：main.py 不废；任何"为了 UI 好做"而砍终端能力的改动一律不许
3. **hook ask 的异步化**（A3）是全计划风险最高的一步：改造后必须回归验证 fail-closed 语义不变
4. **依赖只增不改**：fastapi/uvicorn/pywebview 锁版本；不动 agent_core 既有依赖
5. **零构建承诺**：前端不引 Node 工具链；若未来确需框架，走 Backlog 评估再改本计划

---

## 六、Backlog（UI 相关，只记录不排期）

- 移动端适配（响应式）
- 多会话并行窗口（每个偏殿独立 runner）
- 语音传旨（TTS/STT）
- 国际化（英文 UI）
- 用户画像/记忆的可编辑界面（现在只读）

---

## 七、变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-24 | 创建 UIPLAN.md：Emperor Agent 立项，阶段 A-D 共 12 项任务 | 软件化方向；沿用"计划先行"纪律 |
| 2026-08-24 | 任务 A1 完成并勾选：web/server.py + 占位页 + 依赖锁定 | 阶段 A 开工；红线（只绑回环）首次实测通过 |
| 2026-08-24 | 任务 A2 完成并勾选：新增 agent_core/runner.py（内核 + 事件流 + confirmer 注入），main.py 重写为薄外壳；拦截回复文案中性化 | 双入口共用内核落地；事件模型为 A3 WebSocket 预留 |
| 2026-08-24 | 任务 A3 完成并勾选，**阶段 A 完成**：/ws 端点（队列单写者防并发写交错）+ WSConfirmer（事件→回执→超时驳回）；修复 web/server.py 的 sys.path 自举 | 风险最高的 ask 异步化一次通过；超时分支用独立短超时服务器实例验证 |
| 2026-08-24 | 任务 B1 完成并勾选：index.html + style.css 全套宫廷视觉（纯 CSS/内联 SVG）；修复静态挂载 bug（StaticFiles 挂根，置于所有路由后）；顶栏窄屏换行修复 | 视觉验收方法论：真实浏览器截图 + 视觉模型逐项核对 + 窄视口复检 |
| 2026-08-24 | 任务 B2 完成并勾选：app.js 事件→DOM 映射（textContent-only 防注入）；修复 veil 遮罩点击拦截 bug（.veil[hidden]{display:none}） | 流式证明用"发送与采样同调用"的高频 DOM 轮询（19 增长台阶）；断线测试用杀进程模拟 |
| 2026-08-24 | 任务 B3 完成并勾选，**阶段 B 完成**：runner 请旨叫停（threading.Event：流中断流/工具批后收束）+ ws stop 消息；前端回看暂停、回底钮、叫停钮、誊抄、差事灯笼；资源 URL 版本号根治缓存 | 内核与 UI 同步演进；叫停实测 600 字任务收于 13 字 |
| 2026-08-24 | 任务 C1 完成并勾选：hook_ask→圣旨弹窗（准奏/驳回/倒计时），hook_decision 事件文案修正 | 弹窗视觉与回执协议分别取证；浏览器自动化输入间歇失灵故回执链路用注入法验证 |
| 2026-08-24 | 任务 C2 完成并勾选：REST 面板端点（sessions/memory/personas/team）+ ws 消息（new_session/resume/persona），前端四面板接真数据 | 拉取与推送分工：面板数据 REST 拉、动作走 ws；验证用注入法+浏览器截图双轨 |
| 2026-08-24 | 任务 C3 完成并勾选，**阶段 C 完成**：subagent 事件带摘要、出巡簿/外务府面板、/api/subagent_logs 与 /api/mcp 端点 | 5.2 的日志在 UI 见人；熔断可视化闭环（oxalpha 事件的 UI 答卷） |
| 2026-08-24 | 任务 D1 完成并勾选：run_app.py 桌面壳（uvicorn 后台线程 + pywebview 主线程 + 端口就绪轮询 + 关窗清理链）；pywebview==6.2.1 锁版本 | 阶段 D 开工；首版猴子补丁 serve(sockets=) 签名不兼容改为轮询端口的朴素方案 |
| 2026-08-24 | 任务 D2 完成并勾选：PyInstaller 单文件 exe（38.2MB / 4秒启动）+ 玉玺图标 + 首启 .env 引导；build 产物入 gitignore | 完成标志"无 Python 环境双击可用"以干净目录实测 |
| 2026-08-24 | 任务 D3 完成并勾选，**UIPLAN 12 项全部完成**：截图入库、README/SUMMARY 收录 | 从终端教学项目到桌面软件的全旅程闭环 |
| 2026-08-25 | 发布后修复：黑屏/断连根因是本机常驻软件（O+Connect）抢占 8300 端口；run_app.py 改为端口被占自动改选随机空闲口（connect 探测法，弃用 Windows 下会假阳性的 SO_REUSEADDR 绑定检测）；exe 已重打包 | 用户实测反馈驱动的修复；'固定端口'是桌面软件经典陷阱 |
| 2026-08-25 | 发布后修复②：GBK 内核异常——Windows 中文控制台默认 GBK，模型回复含 emoji 时 print() 抛 UnicodeEncodeError 炸掉内核线程；新增 agent_core/console.py（ensure_utf8_console：stdout/stderr reconfigure 为 UTF-8+replace），三个入口统一接入；exe 重打包 | 用户实测反馈驱动；教训：控制台打印属于 IO 外壳，永远不该有能力杀死内核 |
| 2026-08-25 | 发布后修复③：单派遣子代理分支 UnboundLocalError——runner.py 该分支复制自并发分支，循环变量 block 漏改为 block_id（多路并发与无派遣测试均踩不中，唯"恰好派一个小太监"触发）；一行修复 + 单派遣路径专项验证 + exe 重打包 | 用户问天气时模型恰好单派遣而踩中：分支覆盖盲区的典型样本；并发/单发/无发三路都该有测试 |
| 2026-08-26 | 发布后修复④：工具卡中文变 �——run_shell_command 把子进程输出按 UTF-8 强读，中文 Windows 命令实际是 cp936/GBK 字节，导致成片 U+FFFD；改为先 UTF-8 再 Windows 原生编码（sys.stdout.encoding 而非 locale.getpreferredencoding，后者会被 LANG 污染）再 cp936/mbcs/gbk 兜底；7/7 单测矩阵通过、exe 重打包 | 用户实测驱动；教训：跨编码边界要逐道审计，不能假定'应该'是 UTF-8 |
