# 学习总结（SUMMARY.md）

> 2026-08 · 累积式 Agent 学习实践的全旅程总结
> 路线：学习 TheSyart/claude-agent-examples 与 B站"小单说AI"教程 → 全项目分析 → 制定 PLAN.md → 16 项任务全部完成
> 配套文档：[PLAN.md](PLAN.md)（任务清单、验收标准与变更记录）｜[LEARNING.md](LEARNING.md)（个人笔记与阶段复盘）

---

## 一、项目从起点到终点

**起点**：一个能跑的教学版终端 Agent（工具调用 + 记忆 + 子代理 + 团队 + MCP + Hooks），
但零工程化配套、安全是演示级、存在潜伏 bug。

**终点**：16 项任务完成后——

- **工程化**：README、.gitignore（密钥防泄漏）、requirements.txt 锁版本、LLM 调用异常兜底与分类重试
- **安全**：敏感文件读写管控（deny/ask 分级）、run_command 超时 + 进程树击杀 + Windows 黑名单、web_fetch 三道 SSRF 防线
- **体验**：流式输出、`/todos` `/memory` `/compact` `/new` `/resume` `/persona` 斜杠命令
- **架构**：工具注册表（一处注册全端可用）、队友上下文自动压缩、MCP 长连接断线重连、
  长期记忆 RAG 检索（prompt 体积与记忆量解耦）、多会话管理、人格可配置

---

## 二、16 项任务修改总览

| 任务 | 主要改动 | 关键验证结果 | commit |
|---|---|---|---|
| 1.1/1.2 README + git | 文档、密钥防护、首次推送 | 远程无 .env，运行时文件自动重建 | `ea2a21c` |
| 1.3 requirements.txt | 四依赖锁版本 | 全新 clone + venv 安装启动成功 | `0643424` |
| 1.4 LLM 异常兜底 | call_llm：可重试错误指数退避，其余放弃不崩溃 | 错误 Key 得 401 不退出，正确 Key 重启恢复 | `9604ebd` |
| 2.1 read_file 管控 | ToolPolicyHook matcher 扩展，敏感读取 deny | 单测 12/12，history 密钥泄漏 0 次 | `64ccda4` |
| 2.2 run_command 加固 | 120s 超时（临时文件+taskkill 整树）、Windows 黑名单、命令涉敏感路径 ask | sleep(999) 实测 120.4s 截断、单测 20/20 | `b1a1c1a` |
| 2.3 SSRF 防护 | IP 段直查 + 域名解析后查 + 重定向再查 | 单测 34/34（CIDR 边界、localtest.me、外网不误伤） | `8ea9ec8` |
| 3.1 修 schema bug | TOOLS 补 list_mcp_servers；连带修 mcp 2.0 的 inputSchema 字段名 | 配真实 MCP server 端到端调通（连带挖出第二层潜伏 bug） | `42ce2ed` |
| 3.2 流式输出 | _consume_stream 拼装 chunk，适配器返回同构对象 | 输出文件 9 个增长台阶；5 轮工具调用拼装零错误 | `faa1ac5` |
| 3.3 斜杠命令 | /todos /memory /compact；compact_history 加 force | 全链路实测，自动压缩自然触发两次，原命令零回归 | `b07d667` |
| 4.1 工具注册表 | registry.py 统一 schema+handler；main.py 删 if/elif 长链 | 新增 current_time 零分发改动可用；单测 12/12 | `872fd5e` |
| 4.2 队友压缩 | 工作循环末尾 compact；只追加 episode 不回写共享记忆 | 41→11 条；真实队友水位 22→10 两次、持续回禀 | `79b3878` |
| 4.3 MCP 长连接 | 后台事件循环常驻会话，懒建立+断线重连+atexit | 10 次调用仅 1 次 spawn；杀进程后自动重连恢复 | `e1a5a1f` |
| 4.4 记忆 RAG | memory_rag.py 可插拔向量源 + 阈值混合策略 | 123→403 条记忆 prompt 恒定 2002 字；埋雷双事实全答对 | `96fe700` |
| 4.5 多会话 | sessions.py 全保真存储 + 协议修复 + 懒创建 | 跨进程 /resume 恢复续答；修掉 2 个自产 bug | `1395737` |
| 4.6 persona | 人格模板化，能力条款中性化，/persona 运行时切换 | 能力块两套人格逐字节一致；声音变、工具行为不变 | `52ed7e6` |

---

## 三、核心知识点（按主题重组）

### 1. 工程化基础
- **依赖锁版本**：`包==版本` 保证任何时间任何机器装出同一环境；`.env` 与依赖清单严格分离
- **错误分类重试**：可重试（网络超时/429/5xx → 指数退避 1s→2s→4s）vs 不可重试（401/400 → 立即放弃）；
  **重试语义取决于副作用**——已经打印到屏幕上的内容不能重试（3.2 的流中断不重试）
- **哨兵值传错**：`call_llm` 失败返回 None 而非抛异常，把"怎么办"的决策留给调用方

### 2. Agent 安全线
- **最小权限**：按身份限工具（子代理白名单）、按文件敏感度限操作（.env 读写分级）
- **deny / ask 分级**：无合理场景 → deny（读密钥）；合理但危险 → ask 用户确认（git push、cp .env.example）
- **黑名单是安全带不是围墙**：别名缩写、编码混淆、间接执行三种绕过 → 根治靠白名单+沙箱（纵深防御）
- **防护放在哪一层决定覆盖谁**：Hook 层只护主 Agent（子代理绕过）；工具函数内部护全端（SSRF）
- **SSRF 三道检查**：IP 字面量直查、域名解析后查（防域名指向内网）、重定向再查（防 302 跳内网）
- **必背内网段**：127/8、10/8、172.16-31（/12 的陷阱）、192.168/16、169.254/16（云元数据）、::1、fe80::/10

### 3. LLM 协议与调试
- **schema 是模型的工具宇宙**：tools 数组 = name（寻址）+ description（选型）+ parameters（契约）；
  提示词提到但 schema 缺失 → 模型永远调不出（沉默故障）
- **流式协议**：content 是小段文本增量；tool_calls 更碎（id/name 只来一次，arguments 任意切碎，
  多调用靠 index 区分）→ 按 index 键控累加；输出重定向到文件时必须 flush=True（块缓冲陷阱）
- **消息配对不变量**：role="tool" 必须紧跟带 tool_calls 的 assistant——压缩切分要落在 user 边界、
  会话恢复要修复悬空调用、旧 history.jsonl 因丢配对信息而结构上不可回放
- **从未执行过的代码路径必须假定是坏的**（3.1 两层潜伏 bug 的教训）

### 4. 上下文工程
- **token 预算意识**：不压缩的历史每轮全量重发，成本随轮数**平方级**增长，最终撑爆窗口
- **RAG 取舍**：全量注入"每轮全知但越来越贵" vs 检索注入"只见 Top-K 但恒定便宜"；
  小记忆（≤阈值）全量、大记忆检索的**混合策略**让新机制零回归上线
- **写入权限分级**：谁看过全部数据，谁才有资格写全量记忆——队友只追加 episode，永不重写 MEMORY.md
- **压缩器读全量 vs 注入走检索**：改写记忆需要完整原文；放什么进 prompt 只需要相关性
- **词法 vs 语义检索**：bigram 抓字面相似、零依赖；embeddings 抓同义改写但依赖供应商接口
  （DeepSeek 无 embeddings → 默认词法 + env 一键切换通道 + 失败自动降级）

### 5. 架构与模式
- **注册表模式 / 开放封闭原则**：对新增工具开放（加一条记录），对分发逻辑封闭（永不再改 if/elif）
- **适配器模式**：流式拼装结果包装成"与整段响应同构"的对象，下游十几处代码零改动
- **上下文参数化**：handler 签名带 sender/prefix，一个函数参数消灭一类重复（主 Agent 和队友共用 send_message）
- **函数内导入破循环依赖**：registry ↔ team；`from . import 模块` 引用让测试 spy 可注入
- **懒建立 + 懒重连**：MCP 会话首次调用才建、断线后下次调用再建（需求驱动恢复）
- **人设与能力分离**：能力条款中性化 + 人格模板用语表映射，换人格零改能力代码
- **会话状态两层**：平面审计日志（人看）vs 全保真会话文件（可回放）

---

## 四、自测题汇总（含参考要点）

> 先自己答，再对要点。答不上来的回到对应代码再看一遍。

**阶段一**

1. *为什么 requirements.txt 要用 `==` 锁版本？* —— 不锁则装最新版，上游 breaking change 让项目莫名失效且无法复现"当时能跑"的环境。
2. *401 和 429 为什么一个不重试一个要重试？* —— 401 是请求本身错误（Key 无效），重试结果相同还烧配额；429 是临时过载，退避后大概率恢复。
3. *call_llm 失败后为什么保留用户消息？* —— 上下文不断裂：用户重问或改问时，Agent 仍知道刚才问过什么。

**阶段二**

4. *read_file(".env") 到收到拒绝经过哪几步？* —— execute_main_tool → HOOKS.emit(before_tool_call, matcher=read_file) → HookRegistry 按 matcher 过滤 5 个 Hook → ToolPolicyHook 命中 SENSITIVE_PATTERNS → deny 短路返回，execute_basic_tool 从未执行。
5. *读 .env 用 deny、git push 用 ask，为什么？* —— Agent 读密钥无正当理由（配置是启动时人配的）；push 有合理场景，值得问一次；且 ask 在非交互环境 fail-closed 默认拒。
6. *子代理调 read_file 会被拦吗？* —— 不会。子代理走 execute_basic_tool，不经过 execute_main_tool 的 Hook 链（已记 Backlog）。
7. *第一版超时为什么卡死？* —— shell=True 下真正的命令是孙进程，继承管道写端；subprocess.run 超时只杀 shell，孙进程握着管道，收尾的 communicate() 等不到 EOF 无限阻塞。修复：输出走临时文件 + taskkill /T 杀整树。
8. *三种绕过黑名单的方式？* —— ①别名/缩写变体（ri -r）；②编码混淆（-EncodedCommand base64）；③间接执行（先写脚本再执行）。
9. *172.16.0.0/12 覆盖哪些？* —— 172.16 ~ 172.31（不是到 172.255）；172.32.0.1 不算内网。
10. *防护写在 Hook 层和工具函数里，覆盖范围差在哪？* —— Hook 层只覆盖主 Agent；工具函数内部覆盖主 Agent、子代理、队友全部调用方。
11. *为什么外网 302 跳内网也必须拦？* —— 检查只发生在初始 URL，重定向目标若不检查，攻击者用一个公网跳板就能借道打内网。

**阶段三**

12. *模型为什么调不出提示词里提到、tools 里没有的工具？* —— 模型对工具的全部知识来自 tools 数组；提示词文字只是描述不是契约，没有 schema 就没有可调用的对象。
13. *inputSchema AttributeError 为什么原作者没遇到？* —— 那段代码只在配置了 MCP server 时执行，原作者从没配过——从未执行的路径里躺着 bug。
14. *流式下 arguments 怎么传？为什么按 index 键控？* —— 切成任意多段陆续到达，id/name 只在首段；多个工具调用的碎片交错到来，index 是唯一归属依据。
15. *重定向到文件时没 flush 会怎样？* —— 块缓冲导致输出攒够才落盘，文件增长曲线变成跳变，流式的观测与验证全部失效。
16. *斜杠命令和模型工具的分界线？* —— 查看/控制程序自身状态的操作不经过模型（免费、即时、私密、确定）；完成任务的事交给模型。
17. */compact 为什么需要 force？* —— 自动压缩有阈值，手动命令语义是"现在就压"；同一函数两种场景，用参数区分语义而非复制代码。

**阶段四**

18. *注册表比 if/elif 好在哪？* —— ①加工具只改一处（消灭多处维护漏改，3.1 的 bug 就是这类）；②分发逻辑封闭，回归风险不随工具数增长；③三端（主/队友/命令）共用一张表。
19. *send_message 为什么需要 sender？* —— 主 Agent 以 "lead" 名义发、队友以自己名字发；上下文作为参数传入，一个 handler 服务所有调用方。
20. *队友不压缩为什么平方级？* —— 第 N 轮上下文 ≈ 全部历史，每轮全量重发，总量 ≈ 消息长度 × 轮数²/2；且终将超窗口导致队友死亡且无法自愈。
21. *为什么队友只追加 episode？* —— 队友只见局部，全量重写会用自己的片面视角冲掉主 Agent 的完整记忆（"传令兵覆盖了用户画像"）。
22. *spy 为什么能生效？* —— `from . import memory_compact` 是模块引用，运行时查属性，替换模块属性即可拦截；直接导入函数会把引用焊死。
23. *旧版 MCP 为什么必须冷启动？* —— asyncio.run 的语义是"建循环→跑完→销毁循环"，会话绑定在循环上随之死亡，只好每次重拉进程。
24. *为什么懒重连不立即重连？* —— 失败当下无法区分"断线"还是"工具报错"，盲目重试可能连环撞墙；下次有需求再重建，需求驱动恢复。
25. *全量注入 vs 检索注入的取舍？* —— 见知识点 4；一句话：每轮全知但越来越贵 vs 每轮局部但恒定便宜，生产几乎都选后者再补召回。
26. *bigram 和 embeddings 各抓不住什么？* —— bigram 抓不住同义改写（"色彩偏好"检不中"蓝色"）；embeddings 需要接口可用且按量计费。
27. *旧 history.jsonl 为什么不能 resume？* —— 只存 role/content，丢了 tool_calls/tool_call_id 配对，回放违反消息配对协议必被 API 拒绝。
28. *会话中途崩溃为什么要修复？* —— 末尾悬空 tool_calls（有调用无结果），直接回放违反协议；load 时补占位 tool 结果让脏数据止步于入口。
29. *"测试通过"和"功能在工作"差了什么？* —— 假绿灯事件：模型自己翻文件答对了问题，答案对了机制全错；必须核对旁证（文件增长、状态打印），不止看结果。
30. *人设能力分离后，太监腔调从哪来？* —— 人格模板的用语表把中性词映射回宫廷用语（任务→差事、子代理→小太监）；能力条款保持中性不变。

---

## 五、踩坑实录（比代码更值钱的部分）

| # | 事件 | 现象 → 根因 → 教训 |
|---|---|---|
| 1 | 完成标准撞上架构现实（1.4） | "改 Key 免重启恢复"做不到——client 启动时读 .env → **修订计划并留痕，不放水标准** |
| 2 | Windows 管道死锁（2.2） | 教科书写法 subprocess.run(timeout=) 卡死——孙进程持有管道写端 → **能跑 ≠ 边界条件下能跑；跨平台系统编程要实测** |
| 3 | 两层潜伏 bug（3.1） | schema 缺失是沉默故障 + inputSchema 字段名崩在从未执行的路径 → **从未端到端验证过的功能必须假定是坏的** |
| 4 | DeepSeek 无 embeddings（4.4） | 三个候选模型全 404 → **能力探测 + 降级 + 切换通道**的组合拳比"等有了再说"务实 |
| 5 | replace_all 自递归（4.5） | 全局替换把 remember 定义体内的调用也换了，无限递归写 989 条后崩，堆栈 misleading 地炸在 pathlib → **定义和调用点同形时 replace_all 必须逐处确认；同秒数百条重复日志先怀疑递归** |
| 6 | 假绿灯（4.5） | `/resume` 精确匹配漏掉带参输入，命令落入聊天；模型自己翻文件"假恢复"答对了问题 → **绿灯也要核对旁证；带参命令用前缀匹配** |
| 7 | 同秒会话撞名（4.5） | 撞名检查只看磁盘，懒创建下同秒双开会拿同一 ID → 单测抓住；**磁盘+内存双重检查** |
| 8 | 绕过链路发现（2.1/2.2/2.3） | type .env 绕读保护、curl 绕 SSRF、子代理绕 Hook——全是验证时亲眼所见 → **纵深防御不是口号，每层都得有人守；堵一门看三窗** |

---

## 六、验证方法学工具箱

1. **spy / monkey patch**：替换模块属性计数或记录（spawn 次数、压缩水位）；前提是代码用模块引用调用
2. **埋雷验证**：把事实埋进 123 条记忆，只有挤进 Top-K 才可能被答对——答对即证明全链路
3. **采样增长曲线**：0.5s 采样输出文件字节，流式=多台阶、整段=一次跳变（体感性质的物理量化）
4. **逐字节不变性**：重构"行为不变"的最强标准（两套 persona 下能力块 1856 字完全一致）
5. **主动制造故障**：杀子进程测断线重连、改错 Key 测异常兜底、写悬空消息测协议修复
6. **旁证核查**：不止看答案，核对文件增长、状态打印、计数变化——防假绿灯
7. **边界精查**：CIDR 边界（172.15/16/31/32）、同秒撞名、空态/满态——边界值最容易暴露 off-by-one

---

## 七、数字一览

- **16/16** 任务完成，**18** 个 commit，约 **百项断言**的单元/集成/端到端验证
- 安全：敏感读取 history 泄漏 **0** 次；sleep(999) 实测 **120.4s** 截断；SSRF 单测 **34/34**
- RAG：123 条 → 403 条记忆，system prompt 恒定 **2002 字**；注入块仅全量的 **2%**
- 队友压缩：水位曲线 **22→10**，两次触发，压缩后持续回禀
- MCP 长连接：10 次调用 + list_tools 仅 **1 次**进程启动；断线重连 spawn=**2**
- 流式：输出增长 **9 个台阶**；连续 **5 轮**工具调用拼装零错误
- 踩坑现场：自递归 **989** 条重复消息（教训无价）

---

## 八、遗留事项与下一步

**Backlog（6 条，详见 PLAN.md）**：llm client 运行时重建；run_subagent 异常兜底；子代理接入 Hook 链；
命令级沙箱（根治 curl 绕过）；批量工具调用一个被拦全轮终止；轮级异常兜底；schema 描述人设用语中性化。

**候选新方向**：Web UI、容器化部署、接入有 embeddings 的供应商对比词法检索效果、
把 spy/埋雷等方法沉淀成正式测试套件（pytest）、阶段复盘写作（LEARNING.md 四个阶段还空着）。

**老规矩**：动代码之前先改 PLAN.md。
