# 学习记录（LEARNING.md）

> 与 PLAN.md 的【学习路线】配套使用：**每勾选一个开发任务，就按模板在下方追加一条笔记。**
> 规则：必须用自己的话写，不抄概念定义；写不出来就说明还没懂，回代码里再读一遍。

> **补记说明（2026-09-19）**：任务级笔记没坚持下来（"每勾选一个任务写一笔记"的纪律执行到
> 阶段五前后就断了），此地为欠账一次性还清——阶段一~九的复盘依据 PLAN 变更记录、SUMMARY 与
> git 留痕重写，阶段十~十三为当日实际过程记录。重写的"我的理解"是二手的，建议隔段时间
> **用自己的话再改写一遍**：写不出来就回代码里再读一遍，这正是本文件存在的意义。

---

## 记录模板（复制这段使用）

### YYYY-MM-DD · 任务编号 · 任务名
- **做了什么**：（改了哪些文件、遇到什么问题、怎么解决的）
- **我的理解**：（3~5 句话讲清这个任务背后的原理）
- **还没搞懂 / 想验证的**：（留给下次）

---

## 学习笔记

（任务级笔记未坚持，改以阶段复盘承载；逐知识点的理解校验走 SUMMARY.md 的自测题——
答不上的题回代码里复读，比笔记更接近"检验是否真懂"。）

---

## 阶段复盘（每完成一个阶段写一次）

### 阶段一复盘 · 工程化底座（补记）
- **做了什么**：REPL 主循环 + 工具分发 + function calling 打通；1.4 给 LLM 调用加了兜底——当时只想到"调模型会失败"，给后来埋了伏笔。
- **我的理解**：Agent 的本体就是"LLM→工具→LLM"的 while 循环，其余全是周边设施；工具结果以 `role=tool` 配对回填是整个协议的枢纽，后面所有故事（压缩切分、会话恢复、异常兜底）都在维护这条不变量。
- **还没搞懂（→后来）**：只兜了 LLM 段，轮内其他炸点全裸奔——这笔账到**阶段十一**才还清。

### 阶段二复盘 · 安全加固（补记）
- **做了什么**：Hook 四层模型（Event→Matcher→Handler→Decision）、敏感文件 deny/ask 分级、SSRF 三道检查（IP 字面量→解析后→重定向）、命令超时与进程树击杀。
- **我的理解**：安全不是一道墙而是一组闸门——deny 给"无合理场景"（读密钥），ask 给"有合理场景但危险"（git push）；**防护做在哪一层决定覆盖谁**（这句话在阶段十变成了主线剧情）；大小写不敏感匹配是被"变体绕过"教育出来的。
- **还没搞懂（→后来）**：① 子代理绕过 Hook 链（→**阶段十**收编）；② curl 绕过 SSRF（→**阶段十二** ask 层，根治待容器沙箱）。两条都是端到端实测里 Agent 自己"演示"出来的。

### 阶段三复盘 · 修 Bug 与体验（补记）
- **做了什么**：修"提示词提到但 schema 缺失"的沉默故障、流式 tool_calls 按 index 拼装、压缩 force 参数。
- **我的理解**：**从未执行过的代码路径必须假定是坏的**——schema 兼容 bug 和字段名 bug 都是"写的时候觉得对、第一次真跑就崩"；流式协议里 tool_calls 比 content 碎得多（id/name 来一次、arguments 任意切碎），按 index 键控累加是唯一正解。

### 阶段四复盘 · 架构演进（补记）
- **做了什么**：registry 注册表（一处注册全端可用）、持久 team + inbox 消息总线、MCP 长连接（专职事件循环）、RAG Top-K 注入、多会话全保真存储、persona 模板化。16 项任务收官。
- **我的理解**：注册表是开放封闭原则的活体——新增工具只写一条注册记录，分发逻辑封死；RAG 解决的是"记忆资产与 prompt 体积解耦"（123→403 条记忆而 prompt 恒定）；共享可变状态要**写权限分级**（队友只追加 episode，永不重写 MEMORY.md）。
- **还没搞懂（→后来）**：批量拦截早退（→**③待做**）、schema 人设用语（→**⑥待做**）两笔账记进了 Backlog；踩了 `replace_all` 自递归 989 条消息的大坑——对全局替换工具要心存敬畏。

### 阶段五复盘 · 容错与可观测（补记）
- **做了什么**：oxalpha 实战复盘三件套——连续失败熔断（34 秒收兵 vs 烧满 15 回合）、子代理执行日志落盘、错误文案引导换源。
- **我的理解**：容错的本质是**把无信息量的失败串换成可归因的信息**；熔断阈值用"连续"不用"累计"（累计会误杀"多次失败后成功"的健康探索）；错误消息也是提示词——"建议换可达源"直接改变模型行为。
- **还没搞懂（→后来）**：子代理自己的 LLM 调用还是裸的（→**⑤待做**）——熔断管工具失败，管不了 API 抛错。

### UI 阶段复盘 · Emperor Agent 软件化（A~J 合并，补记）
- **做了什么**：FastAPI 骨架（只绑 127.0.0.1）→ WebSocket 事件流 → 宫廷风前端（纯 CSS 零构建）→ 面板群 → pywebview 壳 + PyInstaller 单文件 → 体验打磨（Markdown 渲染器、圣旨弹窗升级）→ 模型阁/内务府 → 会话三件套/驻留条/暂存冲队 → 昼间双主题「素绢明堂」。
- **我的理解**：**哑内核、聪明外壳**（内核只发事件不打印）+ **confirmer 注入**（依赖倒置：内核声明需要确认能力，外壳决定它长什么样）是"同一内核多种外壳"的全部秘密；XSS 防线从渲染层做起（textContent-only，工具输出是不可信输入）；打包三戒（密钥绝不打包 / 动态导入手动 hiddenimports / 端口协调用轮询笨办法）；CSS 变量先行让双主题成为覆盖块而不是重写。
- **踩坑**：veil 遮罩拦点击、StaticFiles 未挂载、断线 onopen 忘重置 busy——UI 的坑多半是"状态机边界"的坑。

### 阶段六复盘 · 模型配置中心
- **做了什么**：model_profiles 多档案（.env 自动种子迁移）+ apply_profile 原地重建 client，换模型零重启。
- **我的理解**：模块级单例要可替换，调用方必须**每次经属性取**（`llm.client` 而非 `from .llm import client`）——一个 import 语句的写法就是架构决策：属性引用是活的，from-import 是焊死的。

### 阶段七复盘 · 人性化交互
- **做了什么**：save_memory 工具 + 压缩事件可见化、会话自动命名（微型 LLM 调用）、另拟/改旨（历史回滚 + truncate + 跳过门禁提醒）、思维链流式转发（不入 history）。
- **我的理解**：记忆要有**存在感**（事件让用户看见写进了什么）用户才敢用；另拟改旨的难点不在回滚而在**显示层与 history 同步收走**；思维链是"给用户看的"不是"给模型的"，不入史是口径问题。
- **踩坑**：推理模型把小 max_tokens 全花在思维链上导致正文为空——上限放宽到 1024。

### 阶段八复盘 · 会话管理与导出
- **做了什么**：会话删除/改名/检索/整卷导出、当值守卫 409、busy 暂存冲队、要务驻留条、终端 /find /export。
- **我的理解**：done 只记账不收工（busy 保持到 idle 再冲队）——**状态机的边界要对齐真实的"办差中"**，不然就有竞态窗口；rename 记 custom_titles 防"朱笔大过老奴题名"，是产品语义写进代码的例子。

### 阶段九复盘 · 内务府设置中心
- **做了什么**：app_settings 两层覆盖（settings.json 用户层覆盖 .env 种子层）+ 强校验（类型收敛 + 区间）+ 三消费点使用时现读零重启。
- **我的理解**："种子层/用户层"分离让 .env 退化为出厂设置；配置生效的正确姿势是**使用时现读**而不是"改完广播刷新"——零重启不靠热更新机制，靠读取时机；上下文窗口刻意不收编（归模型阁）——两个真理来源比一个错误的抽象更危险。

### 阶段十复盘 · Hook 收编（当日实录）
- **做了什么**：Hook 链从 dispatch_tool 下沉到 `registry.execute_guarded` 统一守卫入口，主循环/子代理/队友三端一次收编；ask 无确认者 fail-closed 降级为 deny；审计条目增 sender；熔断计数兼容拒绝消息。
- **我的理解**：**收编优于下沉**——链内聚一处、调用方只注入 confirmer/on_event，防护语义永远只有一个版本（下沉工具层则是把策略撒进每个函数）；"问不了就别做"与终端非交互默认拒绝是同一条 fail-closed 哲学的两个分支。
- **实证**：真跑派小黄门读 .env——两度被拦、一行未泄、小太监还主动拒绝"绕道改名换路径"；25 项断言含"临时计数 Hook 证 before_tool_call 单次触发"。
- **还没搞懂**：子代理的 ask 要不要升级成圣旨弹窗给皇上（现在自动拒绝）——留作内务府"子代理 ask 策略"备选，并发派遣时多弹窗互撞的问题要先想清楚。

### 阶段十一复盘 · 轮级异常兜底（当日实录）
- **做了什么**：`_parse_tool_blocks` 协议保对（坏 JSON 参数就地回 Error tool 消息）；`_finish_round` 安全网（`_crash_landing`：补悬空→error 事件→说明入史→正常走 done）；send/regenerate/edit_last 入口级网；终端 REPL 护栏。
- **我的理解**：悬空 tool_calls 是"一个坏参数放大成一个会话死刑"的机制——协议不变量（role=tool 必须紧跟 tool_calls）被打破后下轮请求轮轮 400；**安全网自身必须全程防御**——兜底路径再炸不能击穿，error 事件先发、落盘动作各自包 try。
- **断言抓到的真 bug**：`_assistant_say` 先改内存史后落盘，落盘失败时内存残留半截状态——修正为"先落盘成功再改内存史"。教训：断言设计时专挑"网自己会不会掉"来写是值得的。
- **还没搞懂**：compact_history 内部若炸会被网兜住，但"压缩失败的重试策略"没做（低频，暂缓）。

### 阶段十二复盘 · 命令出访确认（当日实录）
- **做了什么**：EGRESS_PATTERNS 出访模式表 16 条（curl/wget/urllib/certutil…），命中走 ask(confirm)；位次插在高危之后保既有分类零变化。
- **我的理解**：**黑名单是安全带不是围墙**——拦不住变体编码，但能把"打地鼠"升级成"皇上把关"；SSRF 防护从 web_fetch 窄口子扩成"出网必过闸"；纵深防御 + 如实留痕（拦不住变体写进注释）比假装安全诚实。
- **实证**：真跑"用 curl 访问 192.168.1.1"——模型拼的 `curl -s -i -m 10 http://… | head -50` 管道命令照样被子串匹配逮住，圣旨弹窗亮"网络出访"文案，Esc 驳回零实际出网。
- **还没搞懂（→Backlog ②）**：根治要容器沙箱（`--network none`），属独立立项（需 Docker Desktop 前提）。

### 阶段十三复盘 · 密折（当日实录）
- **做了什么**：临时对话——ephemeral runner（mi- 前缀不落盘）；五个写口子全堵（remember 短路 / 压缩跳过 / 题名跳过 / truncate 跳过 / save_memory+spawn_teammate 禁写）；誊出逃生门（共享渲染，导出重构与旧算法逐字节等价）；终端 /mi + Web 按钮/横幅双主题。
- **我的理解**：**"不留痕"=堵全部写口子，"读记忆照旧"管写不管读**（失忆的密谈体验反而割裂）；runner 随连接生灭让"关窗即焚"天然成立——架构早就备好了这个功能，工作量全在"把口子找全"；审计日志是安全设施不随密折消失，边界写进界面文案比事后解释诚实。
- **实证**：真跑"记住朕喜欢读史书"——save_memory 被拒且老奴如实回禀"开正式偏殿再补录"；MEMORY/history/会话目录三处零残留；**这套验证自己就是用密折跑的，零清理**——功能第一次跑就回报了开发流程。
- **还没搞懂**：浏览器自动化（Playwright）点击该页按钮偶发超时（程序化点击全通）——自动化工具怪癖，未深究。

### 阶段十四复盘 · 债③+债⑤ 合并清偿（当日实录）
- **做了什么**：③批量拦截去早退——同批一成一败时注入"如实汇报其余、勿换路径重试"提醒后继续循环，`_last_real_user_index` 增 SYNTHETIC_USER_PREFIXES 统一跳过合成消息；⑤子代理 LLM 调用包指数退避重试壳（RETRYABLE_ERRORS/MAX_LLM_RETRIES 迁 llm.py，主循环与子代理共用同一元组对象），连败降级为文字回禀 + 出巡簿 outcome="llm_error"。
- **我的理解**：**陪葬是最差的汇报**——一个被拦就终止全轮，等于用固定文案替模型说话、没收同批成功结果；正确姿势是"信息给足（全部 tool 消息在史）+ 围栏明确（注入提醒禁重试）+ 循环继续"。重试口径必须只有一份：两处各养一套"什么算瞬时错误"迟早漂移。**失败降级为字符串**而不是抛异常——字符串能走正常回禀流程，异常会没收并发派遣里其他小太监的成果。
- **实证**：浏览器真跑复现了 4.1 的原场景——同批 current_time ✓ + read .env ✗，模型把两件事都如实汇报（"一、时辰 00:07；二、读取 .env 被宫禁拦下"），还复述了提醒里的话"不敢擅改路径、不敢换名重试"；断言 14 项全绿（fake call_llm/client，不烧真 token）。
- **还没搞懂**：防重试提醒在极端情况下可能被模型无视（连续撞墙）——是否要像 Stop 门禁一样限一次，暂缓观察。
